<div class="w-full max-w-xs">
  <%= form_with(model: hangout, local: true) do |form| %>
    <% if hangout.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(hangout.errors.count, "error") %> prohibited this hangout from being saved:</h2>

        <ul>
          <% hangout.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= form.label :name %>
      <%= form.text_field :name, class: 'shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline' %>
    </div>

    <div class="actions">
      <%= form.submit class: 'btn btn-default mt-5' %>
    </div>
  <% end %>
</div>
